name: Metrics
on:
  # 每天半夜 0 点自动更新
  schedule: [{cron: "0 0 * * *"}]
  # 允许手动触发
  workflow_dispatch:
  # 推送代码时触发
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}

          user: sLingli
          template: classic
          base: "" 
          config_timezone: Asia/Shanghai
          
          # 🐱 开启猫咪插件
          plugin_tamagotchi: yes
          plugin_tamagotchi_symbol: 🐱
          plugin_tamagotchi_gif: yes
          plugin_tamagotchi_carrier: yes
